# Convex Platform - Claude Instructions

## Project Context

Next.js 16 + React 19 + Convex platform for real-time web applications.
Stack: TypeScript (strict), Tailwind CSS v4, Convex backend.

## Commit Workflow

After completing a feature, invoke `/commit` to create a git commit.
The commit command follows the workflow in `.claude/commands/commit.md`.

Always commit after completing a logical unit of work (feature, fix, refactor).

## Git Commit Rules

NEVER add "Co-Authored-By: Claude" lines to commit messages.
